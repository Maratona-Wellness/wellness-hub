# Services

Camada de serviços que contém a lógica de negócio da aplicação.
Cada serviço encapsula operações relacionadas a um domínio específico.

Exemplo de estrutura:

- `tenantService.ts`
- `appointmentService.ts`
- `therapistService.ts`
